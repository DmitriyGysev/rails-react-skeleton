%header.navbar-inverse
  .container
    %nav
      %ul.nav.navbar-nav.pull-right
        %li= link_to "Home"
        - if !user_signed_in?
          %li= link_to('Sign in', new_user_session_path, :method => :new)
          %li= link_to('Sign up', sign_up_path)
        -if user_signed_in?
          %li= link_to('Prifile', edit_user_registration_path)
          %li= link_to('Sign out', destroy_user_session_path, :method => :delete)




